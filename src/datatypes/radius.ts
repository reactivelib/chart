/**
 * @editor
 */
export interface IRadiusPoint{

    /**
     * Radius of the point. Some shapes will use the configured @api{chart.cartesian.ICartesianChartSettings.radiusScales} and this value
     * to determine the radius of the shape.
     */
    r?: number;

}